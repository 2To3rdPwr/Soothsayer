package com.twotothirdpower.commonui.theme

import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily
import com.example.commonui.R

val ACharmingFont = FontFamily(
    Font(R.font.a_charming_font)
)
val BlackNight = FontFamily(
    Font(R.font.black_night)
)
val Celexa = FontFamily(
    Font(R.font.celexa)
)
val CutTheCrap = FontFamily(
    Font(R.font.cut_the_crap)
)
val DarkCollege = FontFamily(
    Font(R.font.dark_college)
)
val DharmaPunk = FontFamily(
    Font(R.font.dharma_punk)
)
val EnchantedLand = FontFamily(
    Font(R.font.enchanted_land)
)
val FinishedSympathy = FontFamily(
    Font(R.font.finished_sympathy)
)
val GhostFactory = FontFamily(
    Font(R.font.ghost_factory)
)
val GraveDigger = FontFamily(
    Font(R.font.grave_digger)
)
val JanjiDilan = FontFamily(
    Font(R.font.janji_dilan)
)
val Joke = FontFamily(
    Font(R.font.joke)
)
val Metamorphous = FontFamily(
    Font(R.font.metamorphous)
)
val NuCenturyGothic = FontFamily(
    Font(R.font.nu_century_gothic)
)
val Odinson = FontFamily(
    Font(R.font.odinson)
)
val OldNewspaper = FontFamily(
    Font(R.font.old_newspaper)
)
val QuarryStone = FontFamily(
    Font(R.font.quarrystone)
)
val TheDefiler = FontFamily(
    Font(R.font.the_defiler)
)
val VirgoDisplay = FontFamily(
    Font(R.font.virgo_display)
)
val WhiteOnBlack = FontFamily(
    Font(R.font.white_on_black)
)
val Zombie = FontFamily(
    Font(R.font.zombie)
)